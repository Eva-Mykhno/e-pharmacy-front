export const selectPharmacies = (state) => state.pharmacies.pharmacies;
export const selectIsLoading = (state) => state.pharmacies.isLoading;
export const selectTotalPages = (state) => state.pharmacies.totalPages;
export const selectPerPage = (state) => state.pharmacies.perPage;
export const selectCurrentPage = (state) => state.pharmacies.currentPage;
